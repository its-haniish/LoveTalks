import React from "react";
import { View, Text } from "react-native";

const HistoryScreen = () => {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
            <Text>Chat and Call history will appear here</Text>
        </View>
    );
};

export default HistoryScreen;
